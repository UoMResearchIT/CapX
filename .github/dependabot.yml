version: 2
registries:
  private-github:
    type: nuget-feed
    url: https://nuget.pkg.github.com/UoMResearchIT/index.json
    token: ${{secrets.ACTIONS_REPO_TOKEN}}

updates:
  - package-ecosystem: "nuget"
    directory: "/PPMTool/"  # Adjust if your .csproj is in a subfolder
    schedule:
      interval: "weekly"
    registries: private-github
