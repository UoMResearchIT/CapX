services:
  capx:
    build:
      context: .
      secrets:
        - github_token
    ports:
      - 80:80
      - 443:443
    volumes:
      - state:/app/state

volumes:
  state:

secrets:
  github_token:
    environment: GITHUB_TOKEN
