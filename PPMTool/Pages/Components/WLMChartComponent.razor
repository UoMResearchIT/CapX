@using PPMTool.Data
@using PPMTool.Enums
@using System.Diagnostics

<RadzenColumn Style="min-width: 50rem">
    <RadzenCard>
        <ApexChart TItem="WLMWeeklyDataChartItem"
                   Title="@Title"
                   Options="@ChartOptions"
                   XAxisType="XAxisType.Datetime"
                   OnDataPointSelection="@(args => OnDataPointSelected?.Invoke(args, Title))"
                   @ref="chart">

            @if (CompareToWorkloadModel)
            {
                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(1).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(1).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(1).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(2).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(2).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(2).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(3).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(3).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(3).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(4).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(4).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(4).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(5).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(5).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(5).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(6).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WLMNetByDuty.ElementAt(6).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(6).GetBackgroundColourCode()" />
            }
            else
            {
                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(1).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(1).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(1).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(2).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(2).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(2).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(3).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(3).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(3).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(4).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(4).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(4).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(5).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(5).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(5).GetBackgroundColourCode()" />

                <ApexPointSeries TItem="WLMWeeklyDataChartItem"
                                 Items="ChartItems"
                                 SeriesType="@SeriesType.Bar"
                                 Name="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(6).GetDescription()"
                                 XValue="@(e => e.WeekStart.ToUnixTimeMilliseconds())"
                                 YAggregate="@(e => Convert.ToDecimal(e.Sum(e => e.WeeklyValuesByDuty.ElementAt(6).Value)))"
                                 Color="@ChartItems.First().WeeklyValuesByDuty.Keys.ElementAt(6).GetBackgroundColourCode()" />
            }
        </ApexChart>
        <hr />
        <RadzenText Text="Average for the Period" TextAlign="TextAlign.Center" TextStyle="TextStyle.H5" />
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        @foreach (var duty in Enum.GetValues<Duty>().Where(x => x != Duty.Other))
                        {
                            <th><RadzenText Text="@duty.GetDescription()" TextAlign="TextAlign.Center" TextStyle="TextStyle.Subtitle2" Style="@($"color: {duty.GetBackgroundColourCode()}")" /></th>
                        }
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        @foreach (var duty in Enum.GetValues<Duty>().Where(x => x != Duty.Other))
                        {
                            <td>
                                <RadzenText Text="@GetTableValue(duty).ToString()" TextAlign="TextAlign.Center" Style="@($"color: {(Math.Abs(GetTableValue(duty)) > 0.1 ? "red" : "green")};")" TextStyle="TextStyle.Subtitle1" />
                            </td>
                        }
                    </tr>
                </tbody>
            </table>
        </div>
    </RadzenCard>
</RadzenColumn>

@code
{
    [Parameter]
    public Action<SelectedData<WLMWeeklyDataChartItem>, string> OnDataPointSelected { get; set; }

    [Parameter]
    public ApexChartOptions<WLMWeeklyDataChartItem> ChartOptions { get; set; }

    [Parameter]
    public IEnumerable<WLMWeeklyDataChartItem> ChartItems { get; set; }

    [Parameter]
    public string Title { get; set; }

    [Parameter]
    public bool CompareToWorkloadModel { get; set; }

    [Parameter]
    public bool NormalisedByTotalHours { get; set; }

    [Parameter]
    public bool UseStackedBars { get; set; }

    public float? Min { get; private set; }
    public float? Max { get; private set; }

    private ApexChart<WLMWeeklyDataChartItem> chart;
    private bool normalisedByTotalHours;
    private bool compareToWorkloadModel;
    private bool useStackedBars;

    /// <summary>
    /// In response to changes in the parameters to the component update local values and execute methods
    /// </summary>
    protected override void OnParametersSet()
    {
        base.OnParametersSet();

        var updateMinMax = false;
        if (NormalisedByTotalHours != normalisedByTotalHours)
        {
            normalisedByTotalHours = NormalisedByTotalHours;

            foreach (var item in ChartItems)
            {
                // Change the normalisation on the data
                item.SwitchNormalisation(normalisedByTotalHours);

                // Recompute the net values
                item.UpdateWLMNetValues(normalisedByTotalHours);
            }
            updateMinMax = true;            
        }

        if (CompareToWorkloadModel != compareToWorkloadModel)
        {
            compareToWorkloadModel = CompareToWorkloadModel;
            updateMinMax = true;
        }

        if (UseStackedBars != useStackedBars)
        {
            useStackedBars = UseStackedBars;
            updateMinMax = true;
        }

        // Recompute min-max
        if (updateMinMax)
        {
            UpdateMinMax();
        }

        // Update the chart options with the new object
        chart?.UpdateOptionsAsync(true, false, false);
    }

    /// <summary>
    /// Updates the vertical axis limits based on the limits of the individual chart items
    /// </summary>
    private void UpdateMinMax()
    {
        if (!compareToWorkloadModel)
        {
            if (normalisedByTotalHours)
            {
                Min = 0;
                Max = 1;
            }
            else
            {
                Min = 0;
                Max = (float)Math.Round(Convert.ToDecimal(ChartItems.Max(x => x.WeeklyValuesByDuty.Where(x => x.Key != Duty.Other).Sum(x => x.Value))), 1);
            }
        }
        else
        {
            Min = (float)Math.Floor(Convert.ToDecimal(ChartItems.Min(x => x.MinNet) * 100)) / 100f;
            Max = (float)Math.Ceiling(Convert.ToDecimal(ChartItems.Max(x => x.MaxNet) * 100)) / 100f;
        }

        Debug.WriteLine($"** Setting Max = {Max}; Min = {Min}");

        ChartOptions.Yaxis.First().Min = Min;
        ChartOptions.Yaxis.First().Max = Max;
    }

    /// <summary>
    /// Method to get the values in the averages table based on the switch settings
    /// </summary>
    /// <param name="duty"></param>
    /// <returns></returns>
    private double GetTableValue(Duty duty)
    {
        var subset = ChartItems.Where(x => x.TotalHoursForWeek > 0);
        if (subset.Count() == 0) return 0;
        if (compareToWorkloadModel)
        {
            return Math.Round(subset.Average(x => x.WLMNetByDuty[duty]), 2);
        }
        return Math.Round(subset.Average(x => x.WeeklyValuesByDuty[duty]), 2);
    }
}