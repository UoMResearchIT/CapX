@using PPMTool.Data.Entities
@using PPMTool.Enums
@using PPMTool.Data

@foreach (var message in Messages.Where(x => x.Status))
{
    <StatusMessageComponent Message=message />
}

@code {
    [Parameter]
    public IList<StatusMessage> Messages { get; set; }
}
