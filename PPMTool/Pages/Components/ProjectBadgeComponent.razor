@using PPMTool.Data.Enums

<RadzenBadge BadgeStyle="@(ProjectStatus.GetBadgeStyle())"
             Text="@ProjectStatus.ToNiceString()" 
             Style="padding: 4px 10px;"/>

@code {
    [Parameter]
    public ProjectStatus ProjectStatus { get; set; }
}
