@page "/capacity"
@inherits BasePage

@using PPMTool.Data

<div class="container">
    @if (IsLoading)
    {
        <div class="spinner"></div>
    }
    else
    {
        <div class="row">
            <div class="col-auto mr-auto">
                <h1>Capacity</h1>
            </div>
            <div class="col-auto">
                <a class="btn btn-primary" href="/addperson"><i class="oi oi-plus" /></a>
            </div>
        </div>
        <div class="card">
            @if (data == null)
            {
                <p class="lead">No data to show</p>
            }
            else
            {
                @*<ApexChart TItem="CapacityProfile"
                           Title="Capacity"
                           Options=options>
                    
                    <ApexRangeSeries TItem="CapacityProfile"
                                     Items="data"
                                     XValue="@(e => e.Person.Name)"
                                     YMinValue="@(e =>e.StartDate.ToUnixTimeMilliseconds())"
                                     YMaxValue="@(e =>e.EndDate.ToUnixTimeMilliseconds())"
                                     Name="Main Incidents" />

                    <ApexRangeSeries TItem="CapacityProfile"
                                     Items="data"
                                     XValue="@(e => e.Person.Name)"
                                     YMinValue="@(e =>e.StartDate.ToUnixTimeMilliseconds())"
                                     YMaxValue="@(e =>e.EndDate.ToUnixTimeMilliseconds())"
                                     Name="Test Incidents" />

                </ApexChart>*@
            }
        </div>
    }
</div>