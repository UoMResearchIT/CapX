@page "/splittask/{ProjectId:int}/{SubTaskId:int}"
@inherits BasePage
@using PPMTool.Data.Entities
@using PPMTool.Pages.Components
@using PPMTool.Services

<div class="row">
    <div class="col">
        <h1>Split Task @originalAddTaskComponent?.TaskModel.Name</h1>
    </div>
</div>

@if (!EditAuthorised)
{
    <div class="row">
        <ProjectManagerOnlyComponent />
    </div>
}
<hr />

<RadzenStack Gap="1rem" Orientation="Radzen.Orientation.Vertical" JustifyContent="JustifyContent.SpaceBetween" Style="height: 100%;">
    <RadzenText TextStyle="TextStyle.Body1">This task currently runs from <b>@originalAddTaskComponent?.TaskModel.StartDate.ToShortDateString()</b> to <b>@originalAddTaskComponent?.TaskModel.EndDate.ToShortDateString()</b></RadzenText>
    <RadzenStack Orientation="Radzen.Orientation.Horizontal" AlignItems="AlignItems.Center">
        <RadzenText TextStyle="TextStyle.Body1">Choose how to split the task:</RadzenText>
        <RadzenSelectBar TValue="int" @bind-Value="@selectedSplitLogic" Change="@SelectedSplitLogicChanged">
            <Items>
                <RadzenSelectBarItem Text="Date" Value="0" />
                <RadzenSelectBarItem Text="Days" Value="1" />
                <RadzenSelectBarItem Text="Work" Value="2" />
            </Items>
        </RadzenSelectBar>
    </RadzenStack>
    @if(selectedSplitLogic == 0)
    {
        <RadzenStack Orientation="Radzen.Orientation.Horizontal" AlignItems="AlignItems.Center">
            <RadzenText TextStyle="TextStyle.Body1">First day of the new task:</RadzenText>
            <RadzenDatePicker ShowTime="false" DateFormat="dd/MM/yyyy" @bind-Value="@splitDate" />
        </RadzenStack>
    }
    else
    {
        <RadzenStack Orientation="Radzen.Orientation.Horizontal" AlignItems="AlignItems.Center">
            <RadzenText TextStyle="TextStyle.Body1">Number of @(selectedSplitLogic == 1 ? "days" : "hours work") retained in original task:</RadzenText>
            <RadzenNumeric TValue="double?" @bind-Value="@splitValue" Min="@Convert.ToDecimal(0.5)" Step="0.5" />
            <RadzenText TextStyle="TextStyle.Body1">@(selectedSplitLogic == 1 ? " days" : " hours")</RadzenText>
        </RadzenStack>
    }
    <RadzenStack Orientation="Radzen.Orientation.Horizontal" AlignItems="AlignItems.Center">
        <RadzenText TextStyle="TextStyle.Body1">Choose how to share the actuals:</RadzenText>
        <RadzenSelectBar TValue="int" @bind-Value="@selectedActualsLogic" Change="@SelectedActualsLogicChanged">
            <Items>
                <RadzenSelectBarItem Text="Overflow to Split Task" Value="0" />
                <RadzenSelectBarItem Text="Divide by Split Proportion" Value="1" />
                <RadzenSelectBarItem Text="Leave All on Original" Value="2" />
            </Items>
        </RadzenSelectBar>
    </RadzenStack>
</RadzenStack>
<hr />

<div class="row">
    <div class="col-6">
        <AddTask @ref="@originalAddTaskComponent" IsSplit="true" ProjectId="@ProjectId" TaskId="@(SubTaskId ?? 0)" />
    </div>
    <div class="col-6 vr">
        <AddTask @ref="@newAddTaskComponent" IsSplit="true" IsCopy="true" ProjectId="@ProjectId" TaskId="@(SubTaskId ?? 0)" />
    </div>
</div>
<hr />

<div class="row">
    <RadzenStack JustifyContent="JustifyContent.Center" Orientation="Radzen.Orientation.Horizontal" Gap="0.1rem" Style="width:100%">
        <RadzenButton Click="UpdateSubTasks" Text="Update" Icon="refresh" />
        <RadzenButton Click="UpdateAndSave" Text="Update & Save" Icon="check" Disabled=!EditAuthorised />
        <RadzenButton ButtonStyle="ButtonStyle.Danger" Click="DiscardChanges" Text="Discard" Icon="close" />
    </RadzenStack>
</div>
<hr />